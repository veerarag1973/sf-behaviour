# sf-behaviour
CLI that runs behaviour tests against any OpenAI-compatible endpoint. Takes a YAML test case file, runs each case, scores against expected outputs. Built-in scorers: refusal detection, PII leakage, faithfulness. Fails CI on regression.
